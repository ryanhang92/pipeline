# Configure connection to amazon
toch ~/.boto
echo "Add Credneitals"

# Spin up servers
ansible-playbook -vv -i localhost, -e "type=webservers" provision-ec2.yml
ansible-playbook -vv -i localhost, -e "type=webservers" provision-ec2.yml


# establish connection and keys


# Configure servers

    # Set TTL

    # Install Dependencies

    # Place execution scripts

